% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_trace.R
\name{ps_trace}
\alias{ps_trace}
\title{Trace diagnostics for null model mixing}
\usage{
ps_trace(ps, fun = c("nullcat", "quantize"), plot = TRUE, ...)
}
\arguments{
\item{ps}{A \code{phylospatial} object.}

\item{fun}{Character: \code{"nullcat"} or \code{"quantize"}, matching the intended \code{fun} argument
to \code{\link[=ps_rand]{ps_rand()}}.}

\item{plot}{Logical: if \code{TRUE}, plot the traces.}

\item{...}{Additional arguments passed to \code{\link[nullcat:trace_cat]{nullcat::trace_cat()}}, such as \code{method},
\code{n_iter}, \code{thin}, \code{n_chains}, \code{n_strata}, \code{fixed}, \code{stat}, etc.}
}
\value{
An object of class \code{"cat_trace"}. See \code{\link[nullcat:trace_cat]{nullcat::trace_cat()}} for details.
}
\description{
Runs multiple independent chains of the null model randomization algorithm and
records a mixing diagnostic at each step, producing trace plots to assess convergence.
This is a convenience wrapper around \code{\link[nullcat:trace_cat]{nullcat::trace_cat()}} that extracts the
appropriate community matrix from a \code{phylospatial} object.
}
\examples{
\donttest{
if (requireNamespace("nullcat", quietly = TRUE)) {
  ps_bin <- ps_simulate(data_type = "binary")
  tr <- ps_trace(ps_bin, fun = "nullcat", method = "curvecat",
                 n_iter = 2000, n_chains = 5, plot = TRUE)

  ps <- ps_simulate(data_type = "prob")
  tr <- ps_trace(ps, fun = "quantize", method = "curvecat",
                 n_strata = 4, fixed = "cell",
                 n_iter = 2000, n_chains = 5, plot = TRUE)
}
}
}
\seealso{
\code{\link[=ps_suggest_n_iter]{ps_suggest_n_iter()}}, \code{\link[=ps_rand]{ps_rand()}}
}
