% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_trace.R
\name{ps_suggest_n_iter}
\alias{ps_suggest_n_iter}
\title{Suggest number of iterations for null model convergence}
\usage{
ps_suggest_n_iter(ps, fun = c("nullcat", "quantize"), plot = TRUE, ...)
}
\arguments{
\item{ps}{A \code{phylospatial} object.}

\item{fun}{Character: \code{"nullcat"} or \code{"quantize"}, matching the intended \code{fun} argument
to \code{\link[=ps_rand]{ps_rand()}}.}

\item{plot}{Logical: if \code{TRUE}, plot the mixing traces with the suggested burn-in marked.}

\item{...}{Additional arguments passed to \code{\link[nullcat:suggest_n_iter]{nullcat::suggest_n_iter()}} and through to
\code{\link[nullcat:trace_cat]{nullcat::trace_cat()}}, such as \code{method}, \code{n_iter}, \code{n_chains}, \code{n_strata}, \code{fixed}, etc.}
}
\value{
An integer giving the suggested minimum number of iterations, with additional
diagnostic information as attributes. See \code{\link[nullcat:suggest_n_iter]{nullcat::suggest_n_iter()}} for details.
}
\description{
Estimates the number of iterations needed for the null model randomization in
\code{\link[=ps_rand]{ps_rand()}} to reach its stationary distribution, given a dataset and algorithm.
This is a convenience wrapper around \code{\link[nullcat:suggest_n_iter]{nullcat::suggest_n_iter()}} that extracts the
appropriate community matrix from a \code{phylospatial} object. Use this before running
\code{\link[=ps_rand]{ps_rand()}} with \code{fun = "nullcat"} or \code{fun = "quantize"} to choose an appropriate
value for \code{n_iter}. The function runs multiple independent chains of the randomization
algorithm, records a mixing diagnostic at each step, and identifies the point at which
chains stabilize.
}
\examples{
\donttest{
if (requireNamespace("nullcat", quietly = TRUE)) {
  set.seed(123)

  # binary data with nullcat
  ps_bin <- ps_simulate(data_type = "binary")
  ps_suggest_n_iter(ps_bin, fun = "nullcat", method = "curvecat",
                    n_iter = 5000, n_chains = 3, plot = TRUE)

  # quantitative data with quantize
  ps <- ps_simulate(data_type = "prob")
  ps_suggest_n_iter(ps, fun = "quantize", method = "curvecat",
                    n_strata = 4, fixed = "cell",
                    n_iter = 2000, n_chains = 3, plot = TRUE)
}
}
}
\seealso{
\code{\link[=ps_trace]{ps_trace()}}, \code{\link[=ps_rand]{ps_rand()}}
}
